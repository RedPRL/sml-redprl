Def test(#A : exp; #B : [exp].exp) : exp = [
  (x : #A) -> (y : #A) -> #A -> #B[x]
].

Thm hello() : [lam([x].x) ~ lam([x].x) lam([x].x)] by [
  eval-goal;
  ceq/refl
].

Tac mytac() = [
  x <- id;
  y <- welp;
  foo
].
