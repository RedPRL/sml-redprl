Def test(#A : exp; #B : [exp].exp) : exp = [
  (x : #A) -> (y : #A) -> #A -> #B[x]
].

Thm hello{a:dim, b:dim} : [
  hcom{a; b ~> b}(bool; tt; {z}. tt; {z}. ff)
    ~
  lam([x].x) tt
] by [
  eval-goal
].

Tac mytac() = [
  x <- id;
  y <- id;
  id
].
