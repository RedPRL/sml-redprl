Def test(#A : exp; #B : [exp].exp) : exp = [
  (x : #A) -> (y : #A) -> #A -> #B[x]
].

Thm hello() : [
  hcom{1; 0 ~> 1}(bool; y; {z}. tt; {z}. ff)
    ~
  lam([x].x) ff
] by [
  eval-goal;
  ceq/refl
].

Tac mytac() = [
  x <- id;
  y <- welp;
  foo
].
