let
  val thm1 =
    prove `bool by
      exact `ff;
      let
        val thm2 =
          prove `wbool by
            exact `tt;
            refine wbool/eq/tt
          end
      in
        print thm2;
        refine bool/eq/ff
      end
    end

  val thm3 =
    push a, b, c in // pushing fresh names onto the stack
      prove `bool by
        exact `tt;
        refine bool/eq/tt
      end
    end
in
  print thm1;
  print thm3
end
