let
  welp =
    prove `bool by
      let
        x = exact `ff
      in
        let
          welp2 =
            prove `wbool by
              let x = exact `tt
              in refine wbool/eq/tt end
            end
        in
          let asdf = print welp2 in
            refine bool/eq/ff
          end
        end
      end
    end
in
  print welp
end
