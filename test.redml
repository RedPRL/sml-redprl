let
  val thm1 =
    prove `bool by
      exact `ff;
      let
        val thm2 =
          prove `wbool by
            exact `tt;
            refine wbool/eq/tt
          end
      in
        print thm2;
        refine bool/eq/ff
      end
    end
in
  print thm1
end
