Thm EqualityElimTest : [
  (-> [b : bool] (path {_} bool tt (if tt tt ff)))
] by [
  let p : [(if tt tt ff) = tt in bool] = {
    auto
  }.

  fresh x <- elim p:triv;
  [`(-> bool (path {_} bool tt ,x)),
   lam b. <_> `tt];
  auto;
].