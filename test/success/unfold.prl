Def Times(#A; #B) = [
  (* #A #B)
].

Tac Proj1{z:hyp} = [
  decompose <proj1 = x> = z.
  hyp x
].

Tac Proj2{z:hyp} = [
  decompose <proj2 = y> = z.
  hyp y
].

Thm Times/Proj(#A) : [
  a/type : #A type 
  >> (-> (Times bool #A) #A)
] by [
  lam x. 
  unfold Times;
  {Proj2 x}
].

Extract Times/Proj.