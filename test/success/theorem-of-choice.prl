Thm Choice(#i:lvl) : [
  (->
   [a b : (U #i)]
   [r : (-> a b (U #i))]
   [f : (-> [x : a] (* [y : b] ($ r x y)))]
   (*
    [f : (-> a b)]
    (-> [x : a] ($ r x ($ f x)))))
] by [
  lam a, b, r, f.
    {lam x. let {proj1 = y} = f [use x]. use y,
     lam x. let {proj2 = z} = f [use x].
      `z; repeat {inversion}; reduce; auto
    }
].

Print Choice.
