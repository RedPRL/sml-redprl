Thm Category{i:lvl} : [ (univ {lsucc i}) ] by [
  fresh ob, hom, x, y ->
   `(record
     [ob : (univ i)]
     [hom : (-> ob ob (univ i))]
     [idn : (-> [a : ob] ($ hom a a))]); // fill in the rest, but this suffices as an example

  // We'll try to improve 'auto' so that this gets done automatically.
  let mem : [($ ,hom ,y ,y) in (univ i)] = auto.
  elim mem;
  auto
].
