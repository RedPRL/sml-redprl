Def Cmp(#m : exp; #n : exp) : exp = [
  lam([x]. #m (#n x))
].

Def SBool/Not : exp = [
  lam([x]. if/s(x; ff; tt))
].

Thm SBool/Not-Not-Id : [
  Cmp(SBool/Not; SBool/Not) = lam([x].x) in sbool -> sbool
] by [
  auto
].

Thm SBool/Not-Not-Id-Path : [
  paths({_}.sbool -> sbool; Cmp(SBool/Not; SBool/Not); lam([x].x)) true
] by [
  {<i> lam x. hyp x};
  auto
].

Extract SBool/Not-Not-Id-Path.