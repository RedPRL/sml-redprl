Thm Decomposition : [
  (->
   (record [foo : (record [a : bool] [b : (* bool bool)])] [bar : S1])
   bool)
] by [
  lam <foo = <a, b = <proj1>>>.
  hyp proj1
].

Extract Decomposition.

Thm Apply : [
  (-> 
   (-> bool (record [a : S1]))
   S1)
] by [
  lam f.
    let <a> = f `tt.
      hyp a
].