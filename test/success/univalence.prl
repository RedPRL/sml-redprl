Def Not = [
  lam([x]. if/s(x; ff; tt))
].

Def NOT{r:dim} = [
  ia{r}(sbool; sbool; Not; Not)
].

Thm IA/wf{x:dim} : [NOT{x} type] by [
  auto
].
 
Thm IA/beta : [ NOT{0} = bool type ] by [
  auto
].

Print IA/beta.