theorem EqualityKind0(#A) :
  (->
   [ty : (U 0 pre)]
   [a b : ty]
   (= (U 0 hcom) (= ty a b) (= ty a b)))
by {
  lam ty a b => auto
}.

theorem EqualityKind1(#A) :
  (->
   [ty : (U 0 discrete)]
   [a b : ty]
   (= (U 0 kan) (= ty a b) (= ty a b)))
by {
  lam ty a b => auto
}.
