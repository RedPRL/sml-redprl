Thm boolTest : [ (x : bool) -> bool true ] by [
  lam x {
    elim{x};
    [ '{ ff }, '{ tt } ]
  };
  auto
].

Print boolTest.

Thm pathTest : [ paths({x}. S1; base; base) true ] by [
  <x> {
    '{ loop{x} }
  };
  auto
].

Print pathTest.
